<h1>My Tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li><%= check_box("tag", tag.id, {checked: true}) %><%= link_to(task.title, task_path(task)) %></li>
<% end %>
</ul>

<p><%= link_to "create new task", new_task_path %></p>
